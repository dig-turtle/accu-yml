package io.digturtle.gradle.accu.yml

import org.junit.jupiter.api.DisplayName
import org.junit.jupiter.api.Test

/**
 * This is an empty test class to be able to check in the directory structure.
 */
@DisplayName("Empty test")
class EmptyTest{

    /**
     * Run an empty test with a print statement to ensure JUnit 5 is wired up.
     */
    @Test
    fun `should compile`(){
        println("compiled and ran!")
    }
}